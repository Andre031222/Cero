<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.ginit.cero</groupId>
        <artifactId>cero-parent</artifactId>
        <version>0.8.0</version>
    </parent>

    <artifactId>cero-launcher</artifactId>
    <packaging>jar</packaging>
    <name>cero-launcher</name>
    <description>Packages an application and its dependencies into an executable jar using only java.util.jar, with no third-party plugins.</description>

    <dependencies>
        <dependency>
            <groupId>dev.ginit.cero</groupId>
            <artifactId>cero-core</artifactId>
            <version>0.8.0</version>
        </dependency>
        <dependency>
            <groupId>dev.ginit.cero</groupId>
            <artifactId>cero-data</artifactId>
            <version>0.8.0</version>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <!-- Solo esto se filtra: la versión que escribe `cero new` en cada pom generado
                 sale del pom y no hay un número más que acordarse de subir a mano. -->
            <resource>
                <directory>src/main/filtrado</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>run-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <executable>java</executable>
                            <classpathScope>test</classpathScope>
                            <arguments>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>cero.launcher.TestSuite</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
